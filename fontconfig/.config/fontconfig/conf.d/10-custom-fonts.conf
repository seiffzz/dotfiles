<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>

<!-- Define aliases for easier reference -->
<alias>
    <family>Geist</family>
    <prefer><family>Geist</family></prefer>
</alias>
<alias>
    <family>Cairo</family>
    <prefer><family>Cairo</family></prefer>
</alias>

<!-- Prioritize Geist for Latin (and other scripts not explicitly handled) -->
<match target="pattern">
    <test name="family">
        <string>sans-serif</string>
    </test>
    <edit name="family" mode="prepend">
        <string>Geist</string>
    </edit>
</match>
<match target="pattern">
    <test name="family">
        <string>serif</string>
    </test>
    <edit name="family" mode="prepend">
        <string>Geist</string>
    </edit>
</match>
<match target="pattern">
    <test name="family">
        <string>monospace</string>
    </test>
    <edit name="family" mode="prepend">
        <string>Geist</string>
    </edit>
</match>

<!-- Prioritize Cairo for Arabic -->
<match target="pattern">
    <test name="lang" compare="contains">
        <string>ar</string>
    </test>
    <edit name="family" mode="prepend">
        <string>Cairo</string>
    </edit>
</match>

<!-- Fallback for any family -->
<match target="pattern">
    <edit name="family" mode="append">
        <string>Geist</string>
        <string>Cairo</string>
        <string>sans-serif</string> <!-- Good general fallback -->
    </edit>
</match>

</fontconfig>

